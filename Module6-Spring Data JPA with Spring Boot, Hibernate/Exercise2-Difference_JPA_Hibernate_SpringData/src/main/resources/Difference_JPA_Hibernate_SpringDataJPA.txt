JPA:
JPA (Java Persistence API) is a specification that defines standards for ORM.

Hibernate:
Hibernate is an ORM framework and implementation of JPA.

Spring Data JPA:
Spring Data JPA simplifies database operations using Repository interfaces.

Summary:
JPA → Specification
Hibernate → Implementation
Spring Data JPA → Simplified layer over JPA/Hibernate